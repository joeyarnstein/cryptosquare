var encrypt = function(string){
  return string.toLowerCase().replace(/ /g,'').replace(/[!@#$%^&*()-=_+|;':",.<>?']/g,'');
  var numberCharacters = string.length;
  var rowWidth = Math.round(Math.sqrt(numberCharacters));
};
